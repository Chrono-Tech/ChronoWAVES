/* tslint:disable:no-unused-variable */

import { AssetValuePipe } from './asset-value.pipe';

describe('AssetValuePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetValuePipe();
    expect(pipe).toBeTruthy();
  });
});
